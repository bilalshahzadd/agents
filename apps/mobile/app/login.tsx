import {useState} from 'react'; import {View,Text,TextInput,StyleSheet} from 'react-native'; import {router} from 'expo-router'; import {useAuth} from '../lib/auth'; import {theme} from '../lib/theme'; import {Button} from '../components/ui'; export default function Login(){const {login}=useAuth(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');return <View style={s.root}><Text style={s.logo}>SPHERIC <Text style={{color:theme.accent}}>AGENTS</Text></Text><Text style={s.h}>Control plane</Text><Text style={s.sub}>Manage authorized social agents from iOS or Android.</Text><TextInput placeholder="Email" placeholderTextColor={theme.muted} value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" style={s.input}/><TextInput placeholder="Password" placeholderTextColor={theme.muted} value={password} onChangeText={setPassword} secureTextEntry style={s.input}/>{!!error&&<Text style={{color:theme.bad}}>{error}</Text>}<Button title="Sign in" onPress={async()=>{try{setError('');await login(email,password);router.replace('/(tabs)/dashboard')}catch(e){setError(String(e instanceof Error?e.message:e))}}}/></View>} const s=StyleSheet.create({root:{flex:1,backgroundColor:theme.bg,padding:24,justifyContent:'center',gap:12},logo:{color:theme.text,fontWeight:'900',fontSize:20,marginBottom:24},h:{color:theme.text,fontWeight:'800',fontSize:30},sub:{color:theme.muted,marginBottom:12},input:{backgroundColor:theme.panel,borderColor:theme.line,borderWidth:1,borderRadius:10,color:theme.text,padding:14}})
