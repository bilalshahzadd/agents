import {useEffect,useState} from 'react'; import {ScrollView,View} from 'react-native'; import {useAuth} from '../../lib/auth'; import {Card,Screen} from '../../components/ui'; export default function Page(){const {api}=useAuth(),[d,setD]=useState<any>(null);useEffect(()=>{api('/dashboard').then(setD).catch(()=>{})},[]);return <Screen title="Command Center" subtitle="Live operating state"><ScrollView>{d&&<View><Card label="Active campaigns" value={d.activeCampaigns}/><Card label="Connected accounts" value={d.connectedAccounts}/><Card label="Enabled agents" value={d.enabledAgents}/><Card label="Pending approval" value={d.content?.pending_approval??0}/><Card label="30d impressions" value={Number(d.analytics?.impressions??0).toLocaleString()}/></View>}</ScrollView></Screen>}
