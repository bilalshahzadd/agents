import {Text,View} from 'react-native'; import {router} from 'expo-router'; import {useAuth} from '../../lib/auth'; import {Button,Screen,s} from '../../components/ui'; import {theme} from '../../lib/theme'; export default function Page(){const {auth,logout}=useAuth();return <Screen title="Settings"><View style={s.card}><Text style={{color:theme.text,fontWeight:'800'}}>Session</Text><Text style={{color:theme.muted,marginTop:8}}>Organization: {auth?.user?.organizationId}</Text><Text style={{color:theme.muted}}>Role: {auth?.user?.role}</Text></View><Button title="Sign out" secondary onPress={async()=>{await logout();router.replace('/login')}}/></Screen>}
