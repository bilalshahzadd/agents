export async function api(path:string,init:RequestInit={}){ const r=await fetch(`/api/backend${path}`,{...init,headers:{'content-type':'application/json',...(init.headers||{})},cache:'no-store'}); if(r.status===401&&typeof window!=='undefined'&&!location.pathname.startsWith('/login')) location.href='/login'; const data=await r.json().catch(()=>({})); if(!r.ok) throw new Error(data.message??`Request failed: ${r.status}`); return data; }
