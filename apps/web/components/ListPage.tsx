'use client'; import { useEffect,useState } from 'react'; import { api } from '../lib/api';
export default function ListPage({endpoint,columns,actions}:{endpoint:string;columns:{key:string;label:string;render?:(v:any,row:any)=>React.ReactNode}[];actions?:(row:any,reload:()=>void)=>React.ReactNode}){const [rows,setRows]=useState<any[]>([]),[err,setErr]=useState('');const load=()=>api(endpoint).then(setRows).catch(e=>setErr(e.message));useEffect(()=>{load();},[endpoint]);return <>{err&&<div className="error">{err}</div>}<table className="table"><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}{actions&&<th>Actions</th>}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id??i}>{columns.map(c=><td key={c.key}>{c.render?c.render(r[c.key],r):String(r[c.key]??'—')}</td>)}{actions&&<td>{actions(r,load)}</td>}</tr>)}</tbody></table></>}
