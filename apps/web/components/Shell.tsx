'use client'; import Link from 'next/link'; import { usePathname,useRouter } from 'next/navigation';
export default function Shell({children,title,subtitle}:{children:React.ReactNode;title:string;subtitle?:string}){ const path=usePathname(),router=useRouter(); const nav=[['/','Command Center'],['/brands','Brands'],['/campaigns','Campaigns'],['/queue','Content Queue'],['/agents','Agents'],['/accounts','Accounts'],['/research','Research & Trends'],['/analytics','Intelligence'],['/audit','Audit Log']]; return <div className="shell"><aside className="sidebar"><div className="logo">SPHERIC <span>AGENTS</span></div><nav className="nav">{nav.map(([href,label])=><Link key={href} href={href} style={path===href?{background:'var(--panel)',color:'var(--text)'}:{}}>{label}</Link>)}</nav></aside><main className="main"><div className="top"><div><h1 className="h1">{title}</h1>{subtitle&&<div className="sub">{subtitle}</div>}</div><button className="btn secondary" onClick={async()=>{await fetch('/api/auth/logout',{method:'POST'});router.push('/login')}}>Sign out</button></div>{children}</main></div>}
