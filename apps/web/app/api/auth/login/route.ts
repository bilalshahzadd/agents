import { NextResponse } from 'next/server';
export async function POST(req:Request){ const body=await req.text(); const r=await fetch(`${process.env.API_INTERNAL_URL??'http://localhost:4000'}/v1/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body}); const data=await r.json(); if(!r.ok)return NextResponse.json(data,{status:r.status}); const res=NextResponse.json({user:data.user}); const secure=process.env.COOKIE_SECURE==='true'; res.cookies.set('spheric_access',data.accessToken,{httpOnly:true,secure,sameSite:'lax',path:'/',maxAge:data.expiresIn}); res.cookies.set('spheric_refresh',data.refreshToken,{httpOnly:true,secure,sameSite:'lax',path:'/',maxAge:60*60*24*30}); return res; }
