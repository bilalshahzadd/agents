'use client'; import Shell from '../../components/Shell'; import ListPage from '../../components/ListPage'; export default function Page(){return <Shell title="Audit Log" subtitle="Immutable operator and configuration activity."><ListPage endpoint="/audit-logs" columns={[{key:'created_at',label:'Time',render:v=>new Date(v).toLocaleString()},{key:'action',label:'Action'},{key:'entity_type',label:'Entity'},{key:'entity_id',label:'ID'},{key:'actor_user_id',label:'Actor'}]}/></Shell>}
